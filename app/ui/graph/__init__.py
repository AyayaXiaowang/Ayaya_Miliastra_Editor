"""节点图编辑相关 UI 子包。

本子包集中承载“节点图编辑器”的核心界面模块，包括：
- 图场景/视图/撤销栈与 IO 入口
- 节点/连线/端口等图形项
- 节点图库与库页面（节点图列表、模板/实例库等）

注意：
- 仅负责 UI 与交互，不直接读写资源文件，持久化仍由 engine/resources 与上层控制器统一协调。
- 新增图编辑相关组件时，请优先放入本子包，而不是 ui 根目录。
"""


