## 目录用途
- 承载 Todo 子系统的运行态与执行相关逻辑：执行序列规划、执行桥接、运行态状态机（failed/skipped 等）、以及“当前 Todo”解析等。

## 当前状态
- 运行态模块保持尽量无 Qt 依赖，便于测试与复用；UI 层仅消费其输出并呈现。

## 注意事项
- 不使用 `try/except` 吞异常；遇到缺失前置条件应 fail-fast 并给出清晰错误信息。
- 与外部执行/监控协作优先通过 Ports/Protocols 表达，避免直接调用外部对象私有方法。

