# Client 流程控制节点目录

## 目录用途
- 客户端流程控制节点（如双分支、循环等）所在目录，负责图中流程的分支、条件与跳转。

## 当前状态
- 节点库构建由 V2 AST 管线扫描 `@node_spec`（只解析不导入）；运行期需要执行节点函数时，由 `app.runtime.engine.node_impl_loader` 按需加载并注入。

## 注意事项
- 仅实现流程跳转逻辑，不访问具体业务状态。
- 需要新增复杂控制节点时，请保持 `@node_spec` 定义清晰，便于 UI 自动生成端口。


