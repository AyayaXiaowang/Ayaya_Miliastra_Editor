# ocr_templates/2K-125-CN

## 目录用途
2K 档位（≈2560 宽）+ 125% 缩放 + 中文（CN）的 OCR 模板资源，用于 UI 元素模板匹配与 OCR 辅助。

## 当前状态
- 仅包含 `.png` 模板文件及按用途划分的子目录（如 `Node/`）。

## 注意事项
- 同一 profile 内模板必须来自同一分辨率/缩放/UI 主题；不要混用。
- 文件命名清晰、避免空格；建议使用下划线或中划线。
- 不提交临时/训练数据，仅提交可复用模板。
