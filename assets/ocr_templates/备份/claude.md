# ocr_templates/备份

## 目录用途
人工留档的 OCR 模板备份目录（历史/实验/对比用）。该目录名不符合 profile 规则，因此不会被运行时自动扫描/选择。

## 当前状态
- 可能包含旧版本模板与多套 profile 的混合文件，仅用于人工对照。

## 注意事项
- 不要让业务逻辑/代码依赖本目录。
- 如需将其中模板投入使用，请复制到合法 profile 目录（如 `4K-100-CN`）再使用。
