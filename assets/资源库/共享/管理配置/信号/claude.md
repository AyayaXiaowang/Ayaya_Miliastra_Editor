## 目录用途
共享管理配置：信号定义目录（历史路径，现已停用）。

## 当前状态
该目录应保持为空：共享根不再提供任何信号定义，避免导出/绑定时出现作用域与 ID 解析不稳定。

## 注意事项
- 禁止在本目录放置任何 `*.py` 信号定义文件；信号必须放在各自项目存档的 `管理配置/信号/` 下。
- 校验会对该目录执行 fail-fast：发现共享信号定义将直接报错，防止隐式依赖与导出结果漂移。

---
注意：本文件不记录任何修改历史。请始终保持对“目录用途、当前状态、注意事项”的实时描述。
