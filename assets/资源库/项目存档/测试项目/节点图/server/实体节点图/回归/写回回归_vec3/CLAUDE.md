## 目录用途
Vec3 写回/导出口径回归样例：用于验证三维向量（Vec3）常量、连线、列表、字典，以及创建元件（Create_Prefab）Vec 端口形态在写回/导出链路中的一致性。

## 当前状态
- `模板示例_三维向量_Vec3_写回回归.py`：覆盖 Vec3 常量/运算连线/列表/字典与创建元件的 Vec 端口用法，并提供 `__main__` 自检入口。

## 注意事项
- 该目录属于测试资产，只应存在于 `assets/资源库/项目存档/测试项目/` 范围内。
- 修改后建议跑：`python -X utf8 -m app.cli.graph_tools validate-file <节点图路径>`。

