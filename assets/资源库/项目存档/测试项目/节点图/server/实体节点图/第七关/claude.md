## 目录用途
`测试项目/节点图/server/实体节点图/第七关/`：第七关相关的 server 实体节点图回归夹具（Graph Code `.py`）。

## 当前状态
- `关卡实体_第七关_门后流程与亲戚生成.py`：用于回归“自定义变量差值不要建模为同源相减(X-X=0)”等解析与语义建模规则。

## 注意事项
- 修改后必须校验：`python -X utf8 -m app.cli.graph_tools validate-file <节点图路径>`。
- 本目录为回归夹具落点：仅用于测试/诊断，不作为业务项目存档维护。

