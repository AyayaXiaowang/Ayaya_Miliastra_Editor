## 目录用途
项目存档 `示例项目_第七关` 的管理配置根：存放 UI 源码、信号定义与其它可被节点图/导出链路消费的配置资源。

## 当前状态
- `UI源码/`：示例 UI 页面源码（HTML），供预览/扁平化/导出写回。
- `信号/`：示例信号定义（Python），供节点图发送/监听与参数类型校验。

## 注意事项
- 本目录下资源通常会被校验与导出链路直接读取；不要放临时产物。
- 修改后跑一次：`python -X utf8 -m app.cli.graph_tools validate-project --package-id 示例项目_第七关`。
