## 目录用途
历史目录：曾存放项目自定义变量的散落变量文件。
当前仓库已统一收敛为 `自定义变量注册表.py` 单文件真源，本目录应保持为空。

## 当前状态
- `测试基础内容`：自定义变量全部在 `../自定义变量注册表.py` 声明。
- 本目录下若存在任意 `.py`，`validate-project/validate-file` 将 fail-fast（禁止多处真源）。

## 注意事项
- 禁止在本目录新增/修改变量文件；请只修改 `自定义变量注册表.py`。
