## 目录用途
- 示例私有插件：向主窗口注入一个“内部工具”菜单，并演示如何调用控制器方法（导入/导出存档）。

## 当前状态
- `plugin.py`：注册主窗口 hook，在启动后为菜单栏增加：
  - `内部工具` → `导入存档（示例）`
  - `内部工具` → `导出当前存档（示例）`

## 注意事项
- 这是示例代码：可复制为你自己的插件目录并改名（例如 `private_extensions/import_savegame/`）。
- 插件加载失败不会被吞掉（不使用 try/except）；错误会直接抛出，便于快速发现依赖问题。


