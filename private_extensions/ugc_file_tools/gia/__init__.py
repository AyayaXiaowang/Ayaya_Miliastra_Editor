from __future__ import annotations

"""
GIA domain package.

约定：
- 新实现应放在本包内（而不是在 ugc_file_tools 根目录新增 `gia_*.py`）。
- 旧路径（`ugc_file_tools.gia_*`）在迁移期可能仍存在 shim；最终目标是全部迁移到 `ugc_file_tools.gia.*`。
"""

