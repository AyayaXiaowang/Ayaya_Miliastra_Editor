from __future__ import annotations

"""
NodeGraph sub-domain.

说明：
- 这里的 NodeGraph 指 `.gia` 内嵌的 NodeGraph 结构（GraphUnit / pins / edges / variables 等）。
- 迁移自 `ugc_file_tools.node_graph_*` 根级模块，后续新代码应优先使用 `ugc_file_tools.graph.node_graph.*`。
"""

