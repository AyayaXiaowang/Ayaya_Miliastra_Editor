from __future__ import annotations

"""
ugc_file_tools.py_ugc_converter

Python 版 dtype 解码器（原先以“顶层模块 py_ugc_converter”形式被导入）。

为了让 `python -m ugc_file_tools ...` 在任意工作目录下都能稳定运行，
这里将其作为 `ugc_file_tools` 的子包提供，并在内部使用相对导入。
"""


